<UserControl x:Class="WPFGameTest.MVVM.View.SingleplayerGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFGameTest.MVVM.View"
             xmlns:renderer="clr-namespace:WPFGameTest.Renderer"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="125*"/>
            <RowDefinition Height="643*"/>
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="MainCamera" Grid.RowSpan="2">
            <Canvas x:Name="MainCanvas">

            </Canvas>
        </ScrollViewer>
        <renderer:Renderer x:Name="Renderer" Grid.RowSpan="2"/>
        <Button Command="{Binding NavigateMainMenuCommand}" Content="MainMenu" Grid.Row="0" FontSize="30"/>
    </Grid>
</UserControl>
